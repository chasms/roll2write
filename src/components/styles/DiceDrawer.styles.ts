import { css } from "../../../styled-system/css";

export const diceWrapperClass = css({
  position: "relative",
  display: "inline-block",
});

export const editButtonPositionClass = css({
  position: "absolute",
  top: 1,
  right: 1,
});

export const editButtonInnerAdjustClass = css({
  padding: 0,
  fontSize: "xs",
});
